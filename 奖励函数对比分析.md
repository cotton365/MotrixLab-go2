# Unitree Go2 平地行走任务：奖励函数对比分析

## 一、核心差异总览

本文档重点对比 **IsaacLab**、**Motrix 修改前** 和 **Motrix 修改后** 三个版本的奖励函数配置，聚焦于迁移过程中的关键调整。

### 训练算法说明

训练使用 **PPO 算法**（Actor-Critic 方法）：
- **Actor 网络**：输入状态 → 输出动作（告诉机器人 12 个关节如何运动）
- **Critic 网络**：评估当前状态的价值（预测能获得多少未来奖励）
- **训练流程**：机器人尝试走路 → 获得奖励反馈 → PPO 更新网络参数 → 逐步改进策略

重点：我们主要关注**奖励函数的设计**，这是引导机器人学习的关键。

---

## 二、关键奖励项对比

以下只列出**与平地行走任务相关**且**在迁移中有重要调整**的奖励项。

### 📊 主要奖励项权重对比表

| 奖励项 | IsaacLab | Motrix 修改前 | Motrix 修改后 | 说明 |
|--------|----------|---------------|---------------|------|
| **tracking_lin_vel** | 1.5 | 1.0 | **1.5** ✓ | 跟踪线速度（最重要） |
| **tracking_ang_vel** | 0.75 | 0.5 | **0.75** ✓ | 跟踪角速度 |
| **orientation** | -2.5 | 0.0 | **-2.5** ✓ | 姿态惩罚（关键修正） |
| **feet_air_time** | 0.25 | 1.0 | **0.25** ✓ | 足部腾空时间（重要调整） |
| **action_rate** | - | -0.001 | **-0.01** | 动作平滑（10倍增强） |
| **torques** | -0.0002 | -0.00001 | **0.0** | 力矩惩罚（框架差异） |
| dof_acc | -2.5e-7 | -2.5e-7 | -2.5e-7 | 关节加速度（已对齐） |
| lin_vel_z | - | -2.0 | -2.0 | z方向速度惩罚（相同） |
| ang_vel_xy | - | -0.05 | -0.05 | roll/pitch惩罚（相同） |

---

## 三、重点讲解：迁移中的关键修正

### 🎯 1. tracking_lin_vel（线速度跟踪）：1.0 → 1.5

**为什么要改？**
- 这是**最重要的任务目标**：让机器人准确跟随速度指令
- IsaacLab 权重 1.5，说明这是主要优化目标
- Motrix 原版权重只有 1.0，导致机器人对速度指令的重视不够

**实际影响**：
```python
# 假设速度误差为 0.1 m/s
原版奖励 = 1.0 × exp(-0.01/0.25) ≈ 0.96
修正后奖励 = 1.5 × exp(-0.01/0.25) ≈ 1.44
```
修正后，速度跟踪的奖励权重增加 50%，机器人会更努力地跟随速度指令。

---

### 🎯 2. orientation（姿态惩罚）：0.0 → -2.5 ⚠️ 最关键修正

**为什么这是最重要的修正？**

原版 Motrix 竟然**没有姿态惩罚**（权重 = 0），这意味着：
- 机器人可以倾斜、侧着走、甚至趴着走
- 只要速度达标就能得分
- 训练出的步态不自然、不稳定

**修正后的效果**：
```python
# 惩罚公式：penalty = sum(gravity_xy²)
机器人水平 → gravity = [0, 0, -1] → penalty = 0 → reward = 0（不扣分）
倾斜 10° → gravity ≈ [0.17, 0, -0.98] → penalty = 0.029 → reward = -0.073
```

权重 -2.5 确保机器人**必须保持直立**，这是符合真实四足机器人行走的基本要求。

---

### 🎯 3. feet_air_time（足部腾空时间）：1.0 → 0.25

**为什么要降低？**

这个调整不太直观，需要理解奖励函数的实现：

```python
# 实际代码逻辑
reward = (腾空时间 - 0.5秒) × 是否刚接触地面
```

**分析**：
- 原版权重 1.0 **太高**，会鼓励机器人"跳着走"（腾空时间越长越好）
- IsaacLab 在 flat 任务中用 0.25，在 rough 任务中用 0.01
- flat 任务（平地）不需要大步伐，0.25 更合理

**实际影响**：
- 权重降低 → 机器人更注重速度跟踪和姿态稳定
- 避免为了"腾空时间长"而跳跃

---

### 🎯 4. action_rate（动作变化率）：-0.001 → -0.01

**这是有意的增强调整**

虽然 IsaacLab 没有显式的 action_rate 项，但在迁移过程中发现：
- 增强这个惩罚（10倍）让动作更平滑
- 防止网络输出"抽搐"
- 实际训练效果更好

**代码含义**：
```python
penalty = sum((当前动作 - 上一步动作)²)
最终惩罚 = penalty × (-0.01)
```

如果动作突变从 0.5 变到 -0.5：
```
penalty = (0.5 - (-0.5))² = 1.0
reward = -0.01（扣分）
```

---

### 🎯 5. torques（力矩惩罚）：-0.0002 → 0.0 ⚠️ 框架差异

**为什么设为 0？**

这是**唯一有意不对齐的参数**：
- IsaacLab：`-0.0002`（惩罚大力矩，鼓励节能）
- Motrix：`0.0`（关闭该项）

**原因**：
1. IsaacLab 和 Motrix 对力矩的**计算方式不同**
2. 在 Motrix 的 flat 任务中，实验验证设为 0 不影响训练效果
3. 如果直接用 -0.0002，可能因为数值范围不同而产生负面影响

这是**框架差异导致的合理调整**，不是错误。

---

## 四、次要调整（简单带过）

以下奖励项在三个版本中基本相同或已关闭，不是迁移重点：

| 奖励项 | 权重 | 说明 |
|--------|------|------|
| dof_acc | -2.5e-7 | 关节加速度惩罚，三个版本一致 |
| lin_vel_z | -2.0 | 防止跳跃，保持不变 |
| ang_vel_xy | -0.05 | 防止摇晃，保持不变 |
| dof_vel | 0.0 | 关节速度惩罚，已关闭 |
| base_height | 0.0 | 高度惩罚，已关闭 |
| collision | 0.0 | 碰撞惩罚，已关闭 |
| stand_still | 0.0 | 静止惩罚，已关闭 |
| hip_pos | -1 | 髋关节位置，Motrix 特有 |

这些项要么已经对齐，要么对训练影响很小，不需要详细讨论。

---

## 五、迁移总结：三个关键点

### ✅ 1. 任务目标对齐

```
tracking_lin_vel: 1.0 → 1.5 ✓
tracking_ang_vel: 0.5 → 0.75 ✓
```
确保机器人**优先跟随速度指令**，这是行走任务的核心。

### ✅ 2. 约束修正（最关键！）

```
orientation: 0.0 → -2.5 ✓
```
这是**唯一的实质性 bug 修复**。原版没有姿态约束，修正后机器人必须保持直立。

### ✅ 3. 细节优化

```
feet_air_time: 1.0 → 0.25 ✓
action_rate: -0.001 → -0.01 ✓
torques: -0.00001 → 0.0 （框架差异）
```
根据任务特点和框架差异进行的合理调整。

---

## 六、给学生的提示

### 如何理解这些权重？

**主要目标类（正权重，越大越重要）**：
- tracking_lin_vel (1.5)：最重要！
- tracking_ang_vel (0.75)：次要目标
- feet_air_time (0.25)：辅助目标

**约束惩罚类（负权重，绝对值越大越严格）**：
- orientation (-2.5)：必须满足！
- lin_vel_z (-2.0)：不能跳跃
- action_rate (-0.01)：动作要平滑

**正则化类（很小的负权重）**：
- dof_acc (-2.5e-7)：微调，让运动更自然

### 调试技巧

训练时记录每项奖励的值：
```python
print(f"tracking_lin_vel: {reward_tracking:.3f}")
print(f"orientation: {reward_orientation:.3f}")
```

如果发现：
- tracking 奖励很低 → 检查 action_scale 是否太小
- orientation 惩罚很大 → 机器人在摔倒，增加训练时间
- 总奖励不增长 → 可能某个权重设置不合理

---

## 附录：完整权重配置

### IsaacLab (flat_env_cfg.py)
```python
track_lin_vel_xy_exp: 1.5
track_ang_vel_z_exp: 0.75
flat_orientation_l2: -2.5
feet_air_time: 0.25
dof_acc_l2: -2.5e-7
dof_torques_l2: -0.0002
```

### Motrix 修改前 (cfg.py)
```python
tracking_lin_vel: 1.0
tracking_ang_vel: 0.5
orientation: 0.0  # ⚠️ 缺失约束
feet_air_time: 1.0  # 过大
action_rate: -0.001
torques: -0.00001
dof_acc: -2.5e-7
```

### Motrix 修改后 (cfg.py) ✓
```python
tracking_lin_vel: 1.5  # ✓ 对齐
tracking_ang_vel: 0.75  # ✓ 对齐
orientation: -2.5  # ✓ 修复
feet_air_time: 0.25  # ✓ 调整
action_rate: -0.01  # ✓ 增强
torques: 0.0  # ⚠️ 框架差异
dof_acc: -2.5e-7  # ✓ 对齐
```

---

**关键要点**：奖励函数是强化学习的灵魂。通过正确的权重配置，我们引导机器人学会既快速又稳定的行走步态。迁移的核心就是理解 IsaacLab 的设计意图，并在 Motrix 框架中正确实现。
