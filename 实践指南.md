# Unitree Go2 任务迁移 - 图解说明

## 一、整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    强化学习训练循环                            │
└─────────────────────────────────────────────────────────────┘
                            │
                            ↓
        ┌──────────────────────────────────────┐
        │         策略网络 (PPO)                │
        │  输入: observation (状态观察)          │
        │  输出: action ∈ [-1, 1]^12           │
        └──────────────────────────────────────┘
                            │
                            ↓ action (12维)
        ┌──────────────────────────────────────┐
        │       Action Scale 缩放               │
        │  target = action × 0.25 + default    │
        └──────────────────────────────────────┘
                            │
                            ↓ target_pos (目标关节角度)
        ┌──────────────────────────────────────┐
        │          PD 控制器                    │
        │  tau = Kp×(target-pos) + Kd×(-vel)  │
        └──────────────────────────────────────┘
                            │
                            ↓ tau (关节力矩)
        ┌──────────────────────────────────────┐
        │        物理仿真 (Motrix)              │
        │  模拟机器人运动和环境交互              │
        └──────────────────────────────────────┘
                            │
                            ↓
        ┌──────────────────────────────────────┐
        │         奖励函数计算                   │
        │  reward = Σ weight_i × term_i        │
        └──────────────────────────────────────┘
                            │
                            ↓ reward + next_obs
                         返回给策略网络
```

---

## 二、动作空间问题图解

### 错误的动作空间（修改前）

```
以 calf 关节为例：

物理限位: [-2.62, -0.85] rad

动作空间（错误）: [-2.62, -0.85]
                     ▼
策略网络输出: action ∈ [-2.62, -0.85]
                     ▼
乘以 action_scale (0.25):
    0.25 × (-2.62) = -0.655
    0.25 × (-0.85) = -0.2125
                     ▼
加上默认角度 (-1.8):
    -0.655 + (-1.8) = -2.455
    -0.2125 + (-1.8) = -2.0125
                     ▼
最终范围: [-2.455, -2.0125]

问题：
✗ 动作空间不归一化
✗ 范围全是负值，不对称
✗ 与 action_scale 的设计意图不符
✗ 网络很难学习（数值范围奇怪）
```

### 正确的动作空间（修改后）

```
动作空间（正确）: [-1, 1]
                   ▼
策略网络输出: action ∈ [-1, 1]
                   ▼
乘以 action_scale (0.25):
    0.25 × (-1) = -0.25
    0.25 × (1) = 0.25
                   ▼
加上默认角度 (-1.8):
    -0.25 + (-1.8) = -2.05
    0.25 + (-1.8) = -1.55
                   ▼
最终范围: [-2.05, -1.55]

优点：
✓ 动作空间归一化 [-1, 1]
✓ 对称、语义清晰
✓ 0 表示默认姿态，±1 表示最大偏移
✓ 网络容易学习
✓ 与 IsaacLab 一致
```

---

## 三、Go2 机器人关节结构

```
            前方 (Front)
              ↑
        FL          FR
        ┌─┐        ┌─┐
        │ │hip     │ │hip
        └─┘        └─┘
         │          │
        ┌─┐        ┌─┐
        │ │thigh   │ │thigh
        └─┘        └─┘
         │          │
        ┌─┐        ┌─┐
        │ │calf    │ │calf
        └─┘        └─┘
         │          │
        (foot)    (foot)

    ┌────────────────────┐
    │       base         │  ← 机器人身体
    └────────────────────┘

        (foot)    (foot)
         │          │
        ┌─┐        ┌─┐
        │ │calf    │ │calf
        └─┘        └─┘
         │          │
        ┌─┐        ┌─┐
        │ │thigh   │ │thigh
        └─┘        └─┘
         │          │
        ┌─┐        ┌─┐
        │ │hip     │ │hip
        └─┘        └─┘
        RL          RR

4 条腿 × 3 个关节 = 12 个自由度
```

### 各关节功能

- **hip (髋关节)**:
  - 控制腿的左右摆动（abduction/adduction）
  - 范围约 ±60° (±1.05 rad)

- **thigh (大腿关节)**:
  - 控制腿的前后摆动
  - 范围约 -38° ~ 170° (-0.66 ~ 2.97 rad)

- **calf (小腿关节)**:
  - 控制小腿弯曲
  - 范围约 -150° ~ -49° (-2.62 ~ -0.85 rad)
  - **注意：只能向一个方向弯（全负值）**

---

## 四、迁移过程流程图

```
开始迁移
    │
    ↓
┌─────────────────────┐
│ 1. 阅读 IsaacLab 源码│
│   - flat_env_cfg.py │
│   - rough_env_cfg.py│
└─────────────────────┘
    │
    ↓
┌─────────────────────┐
│ 2. 提取关键参数      │
│   - action_scale    │
│   - reward weights  │
│   - command ranges  │
└─────────────────────┘
    │
    ↓
┌─────────────────────┐
│ 3. 修改 Motrix cfg.py│
│   - ControlConfig   │
│   - RewardConfig    │
│   - Commands        │
└─────────────────────┘
    │
    ↓
┌─────────────────────┐
│ 4. 修改 walk_np.py  │
│   - 修复动作空间     │
└─────────────────────┘
    │
    ↓
┌─────────────────────┐
│ 5. 运行训练         │
└─────────────────────┘
    │
    ↓
    训练成功？
    │      │
   是      否
    │      │
    ↓      ↓
   完成   调试
          │
          └─→ 检查参数 → 重新训练
```

---

## 五、参数影响矩阵

| 参数增大 | 对机器人行为的影响 | 对训练的影响 |
|----------|-------------------|-------------|
| action_scale ↑ | 运动幅度增大，更灵活 | 探索空间增大 |
| tracking_lin_vel weight ↑ | 更严格跟踪速度指令 | 主要目标更明确 |
| orientation penalty ↑ | 保持更直立 | 可能牺牲速度跟踪 |
| feet_air_time weight ↑ | 更动态的步态 | 鼓励跳跃式运动 |
| action_rate penalty ↑ | 动作更平滑 | 响应速度变慢 |
| dof_acc penalty ↑ | 关节运动更平滑 | 加速能力受限 |

---

## 六、对比三个版本

### IsaacLab 版本
```
特点：
✓ 成熟稳定
✓ 参数经过充分验证
✓ 文档完善
✓ 社区支持好

配置特点：
- action_scale: 0.25
- 奖励权重均衡
- 速度范围较大（x:2.0, yaw:π）
```

### Motrix 修改前版本
```
特点：
✗ action_scale 太小 (0.05)
✗ 动作空间定义错误
✗ 部分奖励权重不合理

问题：
- 运动受限
- 与 IsaacLab 不等价
- 训练效率可能较低
```

### Motrix 修改后版本
```
特点：
✓ action_scale 对齐 (0.25)
✓ 动作空间修复为 [-1, 1]
✓ 奖励权重已迁移
✓ 经验证训练成功

优化：
- 速度范围收窄（更保守）
- torques 设为 0（框架差异）
- action_rate 增强（更平滑）
```

---

## 七、代码走查示例

### 场景：网络输出 action = [0.5, 0.3, -0.8, ...]

```python
# 1. 策略网络输出
action = np.array([0.5, 0.3, -0.8, ...])  # 12 维，每个 ∈ [-1, 1]

# 2. 检查是否在动作空间内
assert action_space.contains(action)  # ✓ 通过（因为 [-1, 1]）

# 3. 应用 action_scale
action_scale = 0.25
scaled_action = action * action_scale
# = [0.125, 0.075, -0.2, ...]

# 4. 加上默认角度
default_angles = np.array([0.1, 0.9, -1.8, ...])  # 12 个默认值
target_angles = scaled_action + default_angles
# = [0.225, 0.975, -2.0, ...]

# 5. PD 控制器计算力矩
current_pos = get_joint_positions()
current_vel = get_joint_velocities()

error = target_angles - current_pos
tau = Kp * error + Kd * (-current_vel)

# 6. 应用力矩到仿真器
simulator.set_actuator_torques(tau)

# 7. 仿真一步
simulator.step()

# 8. 获取新状态
next_obs = get_observation()

# 9. 计算奖励
reward_terms = {
    "tracking_lin_vel": compute_tracking_reward(...),
    "orientation": compute_orientation_penalty(...),
    # ...
}
total_reward = sum(weight * term for weight, term in reward_terms.items())

# 10. 返回给训练器
return next_obs, total_reward, done, info
```

---

## 八、常见错误和解决方案

### 错误 1: 动作空间超出范围

**症状：**
```
ValueError: action must be in action_space
```

**原因：**
- 网络输出的 action 超出了动作空间定义的范围

**解决：**
- 确保动作空间定义为 `[-1, 1]`
- 确保网络输出层有 tanh 激活函数（输出 [-1, 1]）

### 错误 2: 机器人立即摔倒

**症状：**
- episode 很快结束（base 接触地面）

**原因：**
- 初始姿态不稳定
- 或者 action_scale 太大导致动作剧烈

**解决：**
- 检查 `default_joint_angles` 是否合理
- 降低 action_scale 从 0.25 到 0.15 试试
- 增加 orientation 惩罚权重

### 错误 3: 机器人不动

**症状：**
- 机器人站着不动，或者只是微微摇晃

**原因：**
- action_scale 太小
- 或者 action_rate 惩罚过大

**解决：**
- 增加 action_scale（但不要超过 0.3）
- 降低 action_rate 惩罚权重
- 增加 tracking_lin_vel 奖励权重

### 错误 4: 奖励不增长

**症状：**
- 训练了很多 steps，reward 曲线平坦

**原因：**
- 奖励函数设计不合理
- 或者任务太难

**解决：**
- 检查各项奖励权重是否平衡
- 降低速度指令范围（如改为 [-0.5, 0.5]）
- 检查 observation 是否正确归一化

---

## 九、参数速查表

### 控制参数
| 参数 | 值 | 说明 |
|------|-----|------|
| action_scale | 0.25 | 动作缩放系数 |
| sim_dt | 0.01 | 仿真时间步长 (10ms) |
| ctrl_dt | 0.01 | 控制时间步长 (10ms) |
| max_episode_seconds | 20.0 | 单个 episode 最长时间 |

### 速度指令范围
| 轴 | 最小值 | 最大值 | 单位 |
|----|--------|--------|------|
| x (前进) | -1.0 | 1.0 | m/s |
| y (侧移) | -1.0 | 1.0 | m/s |
| yaw (旋转) | -1.0 | 1.0 | rad/s |

### 主要奖励权重
| 项目 | 权重 | 类型 | 作用 |
|------|------|------|------|
| tracking_lin_vel | 1.5 | 奖励 | 跟踪线速度 |
| tracking_ang_vel | 0.75 | 奖励 | 跟踪角速度 |
| orientation | -2.5 | 惩罚 | 保持直立 |
| feet_air_time | 0.25 | 奖励 | 动态步态 |
| dof_acc | -2.5e-7 | 惩罚 | 平滑运动 |
| action_rate | -0.01 | 惩罚 | 动作平滑 |
| torques | 0.0 | 惩罚 | 力矩限制（已关闭）|

### 默认关节角度 (rad)
| 关节组 | hip | thigh | calf |
|--------|-----|-------|------|
| FL (前左) | 0.1 | 0.9 | -1.8 |
| FR (前右) | -0.1 | 0.9 | -1.8 |
| RL (后左) | 0.1 | 0.9 | -1.8 |
| RR (后右) | -0.1 | 0.9 | -1.8 |

**注意：** hip 关节左右对称（FL/RL 为正，FR/RR 为负）

---

## 十、训练监控指标

### 应该监控的指标

1. **Episode Reward**
   ```
   期望：逐渐上升，最终稳定在较高值
   异常：一直很低，或者剧烈波动
   ```

2. **Tracking Lin Vel Reward**
   ```
   期望：接近权重值 (1.5)
   说明：机器人能准确跟踪线速度指令
   ```

3. **Tracking Ang Vel Reward**
   ```
   期望：接近权重值 (0.75)
   说明：机器人能准确跟踪角速度指令
   ```

4. **Orientation Penalty**
   ```
   期望：绝对值逐渐减小，接近 0
   说明：机器人保持直立
   ```

5. **Episode Length**
   ```
   期望：逐渐增长，最终达到 max_episode_seconds
   异常：很短（频繁摔倒）
   ```

6. **Action Mean/Std**
   ```
   期望：mean 接近 0，std 在 0.3-0.7 之间
   异常：mean 偏离 0（bias），或 std 过小/过大
   ```

### 理想的训练曲线

```
Episode Reward
    │
  2 │                    ╱────────
    │                 ╱╱
  1 │            ╱──╱
    │        ╱─╱
  0 │  ╱──╱
    │╱
 -1 │
    └───────────────────────────→
      0    500K   1M    1.5M  Steps

说明：
- 前期快速上升（学习基本步态）
- 中期逐渐平稳（优化跟踪精度）
- 后期稳定（收敛到最优策略）
```

---

## 十一、实验建议

### 实验 1: 验证动作空间修复

**目标：** 证明修复后的动作空间更合理

**步骤：**
1. 用修改前的代码训练 1M steps
2. 用修改后的代码训练 1M steps
3. 对比：
   - 最终 reward
   - 训练曲线平滑度
   - 实际运动表现

**预期：** 修改后的版本应该表现更好或至少相当

### 实验 2: action_scale 敏感性分析

**目标：** 理解 action_scale 的影响

**步骤：**
测试不同的 action_scale 值：
- 0.1 (小)
- 0.25 (当前)
- 0.5 (大)

**预期：**
- 0.1: 运动受限，难以跟踪速度指令
- 0.25: 平衡点，效果最好
- 0.5: 运动过于剧烈，可能不稳定

### 实验 3: 奖励权重调优

**目标：** 找到更好的权重组合

**步骤：**
1. 基准版本（当前参数）
2. 增强速度跟踪：
   ```python
   "tracking_lin_vel": 2.0,  # 从 1.5 增加
   "tracking_ang_vel": 1.0,  # 从 0.75 增加
   ```
3. 增强稳定性：
   ```python
   "orientation": -5.0,  # 从 -2.5 增加
   "action_rate": -0.02,  # 从 -0.01 增加
   ```

**分析：** 哪个版本的表现最好？

---

## 十二、代码审查清单

迁移完成后，请按照此清单逐项检查：

### cfg.py 检查

- [ ] `ControlConfig.action_scale = 0.25`
- [ ] `Commands.vel_limit` 范围合理（推荐 [-1, 1]）
- [ ] `RewardConfig.scales` 中以下权重正确：
  - [ ] tracking_lin_vel = 1.5
  - [ ] tracking_ang_vel = 0.75
  - [ ] orientation = -2.5
  - [ ] feet_air_time = 0.25
  - [ ] dof_acc = -2.5e-7
  - [ ] action_rate = -0.01
  - [ ] torques = 0.0
- [ ] `InitState.default_joint_angles` 定义完整（12个关节）
- [ ] `Normalization` 参数合理

### walk_np.py 检查

- [ ] `_init_action_space` 方法中：
  - [ ] 使用 `-np.ones(model.num_actuators)`
  - [ ] 使用 `np.ones(model.num_actuators)`
  - [ ] 不使用 `actuator_ctrl_limits`
- [ ] 动作空间 shape 为 `(12,)` (12个关节)
- [ ] observation_space 定义正确

### 训练验证

- [ ] 能成功启动训练（无报错）
- [ ] episode reward 逐渐上升
- [ ] episode length 逐渐增长
- [ ] 机器人能稳定行走
- [ ] 机器人能跟踪速度指令

---

## 十三、故障排查指南

### 问题诊断流程图

```
遇到问题
    │
    ↓
是训练相关？
    │      │
   是      否（环境配置问题）
    │      │
    │      └─→ 检查 cfg.py 参数
    │          检查 walk_np.py 实现
    ↓
reward 不增长？
    │      │
   是      否
    │      │
    │      └─→ 其他训练问题
    │          (learning rate, etc.)
    ↓
检查奖励函数
    │
    ↓
各项 reward term 的值？
    │
    ↓
    ├─→ tracking_* 很低 → 增加权重或降低任务难度
    ├─→ orientation penalty 很大 → 检查初始姿态
    ├─→ 所有项都很小 → 检查 observation 归一化
    └─→ reward 总是负 → 检查惩罚项权重
```

---

## 十四、进阶话题

### 话题 1: Sim-to-Real (仿真到实物)

训练好的策略要部署到真实机器人，需要考虑：

1. **Domain Randomization**
   - 随机化物理参数（质量、摩擦系数等）
   - 随机化观测噪声
   - 在 cfg.py 的 `NoiseConfig` 中配置

2. **Observation Noise**
   ```python
   @dataclass
   class NoiseConfig:
       level: float = 1.0
       scale_joint_angle: float = 0.03
       scale_joint_vel: float = 1.5
       scale_gyro: float = 0.2
       # ...
   ```

3. **安全检查**
   - 限制最大力矩
   - 碰撞检测和保护
   - Emergency stop 机制

### 话题 2: 从 Flat 到 Rough Terrain

如果要迁移崎岖地形任务：

1. **额外的观测**
   - height scan (高度扫描)
   - terrain type (地形类型)

2. **额外的奖励项**
   - undesired_contacts (不期望的接触)
   - stumble (绊倒)

3. **Curriculum Learning**
   - 从简单地形逐渐过渡到困难地形
   - 动态调整任务难度

### 话题 3: 多模态指令

扩展到支持更多指令类型：

1. **高层指令**
   - "前进"、"后退"、"左转"、"右转"
   - 映射到速度指令

2. **轨迹跟踪**
   - 跟踪预定义的路径
   - 需要额外的路径规划

3. **行为模式切换**
   - 行走、小跑、奔跑
   - 不同的步态模式

---

## 附录 A: 完整的配置文件示例

### cfg.py (修改后的完整版本)

```python
import os
from dataclasses import dataclass, field

from motrix_envs import registry
from motrix_envs.base import EnvCfg

model_file = os.path.dirname(__file__) + "/xmls/scene_flat.xml"


@dataclass
class NoiseConfig:
    """观测噪声配置"""
    level: float = 1.0                  # 噪声总体水平
    scale_joint_angle: float = 0.03     # 关节角度噪声标准差
    scale_joint_vel: float = 1.5        # 关节速度噪声标准差
    scale_gyro: float = 0.2             # 陀螺仪噪声标准差
    scale_gravity: float = 0.05         # 重力向量噪声标准差
    scale_linvel: float = 0.1           # 线速度噪声标准差


@dataclass
class ControlConfig:
    """控制器配置"""
    # 动作缩放：目标角度 = action_scale × action + 默认角度
    action_scale = 0.25  # ← 关键参数！与 IsaacLab 对齐


@dataclass
class InitState:
    """初始状态配置"""
    pos = [0.0, 0.0, 0.42]  # 初始位置 (x, y, z)

    # 12 个关节的默认角度（站立姿态）
    default_joint_angles = {
        "FL_hip": 0.1,
        "FL_thigh": 0.9,
        "FL_calf": -1.8,
        "FR_hip": -0.1,
        "FR_thigh": 0.9,
        "FR_calf": -1.8,
        "RL_hip": 0.1,
        "RL_thigh": 0.9,
        "RL_calf": -1.8,
        "RR_hip": -0.1,
        "RR_thigh": 0.9,
        "RR_calf": -1.8,
    }


@dataclass
class Commands:
    """速度指令配置"""
    vel_limit = [
        [-1.0, -1.0, -1.0],  # min: vel_x, vel_y, ang_vel
        [1.0, 1.0, 1.0],     # max
    ]


@dataclass
class Normalization:
    """观测归一化配置"""
    lin_vel = 2      # 线速度归一化系数
    ang_vel = 0.25   # 角速度归一化系数
    dof_pos = 1      # 关节位置归一化系数
    dof_vel = 0.05   # 关节速度归一化系数


@dataclass
class Asset:
    """资产配置（碰撞检测等）"""
    body_name = "base"
    foot_name = "foot"
    penalize_contacts_on = ["thigh", "calf"]  # 这些部位接触地面会被惩罚
    terminate_after_contacts_on = [           # 这些部位接触地面会终止 episode
        "base_collision_0",
        "base_collision_1",
        "base_collision_2",
        "fl_hip_0",
        "fr_hip_0",
        "rl_hip_0",
        "rr_hip_0",
    ]
    ground = "floor"


@dataclass
class Sensor:
    """传感器配置"""
    local_linvel = "local_linvel"  # 本地坐标系线速度传感器
    gyro = "gyro"                  # 陀螺仪


@dataclass
class RewardConfig:
    """奖励函数配置"""
    scales: dict[str, float] = field(
        default_factory=lambda: {
            "termination": -0.0,           # 终止惩罚（已关闭）
            "tracking_lin_vel": 1.5,       # 线速度跟踪（主要目标）
            "tracking_ang_vel": 0.75,      # 角速度跟踪（次要目标）
            "lin_vel_z": -2.0,             # z 方向速度惩罚（防止跳跃）
            "ang_vel_xy": -0.05,           # roll/pitch 角速度惩罚
            "orientation": -2.5,           # 姿态偏离惩罚（保持直立）
            "torques": -0.0,               # 力矩惩罚（框架差异，设为0）
            "dof_vel": -0.0,               # 关节速度惩罚（已关闭）
            "dof_acc": -2.5e-7,            # 关节加速度惩罚（平滑运动）
            "base_height": -0.0,           # 高度惩罚（已关闭）
            "feet_air_time": 0.25,         # 足部腾空奖励（动态步态）
            "collision": -0.0,             # 碰撞惩罚（已关闭）
            "feet_stumble": -0.0,          # 绊倒惩罚（已关闭）
            "action_rate": -0.01,          # 动作变化率惩罚（平滑控制）
            "stand_still": -0.0,           # 静止惩罚（已关闭）
            "hip_pos": -1,                 # hip 关节位置惩罚
            "calf_pos": -0.0,              # calf 关节位置惩罚（已关闭）
        }
    )

    tracking_sigma: float = 0.25      # 跟踪奖励的高斯核宽度
    max_foot_height: float = 0.1      # feet_air_time 的最大高度阈值


@registry.envcfg("go2-flat-terrain-walk")
@dataclass
class Go2WalkNpEnvCfg(EnvCfg):
    """Go2 平地行走环境配置（主类）"""
    max_episode_seconds: float = 20.0
    model_file: str = model_file
    noise_config: NoiseConfig = field(default_factory=NoiseConfig)
    control_config: ControlConfig = field(default_factory=ControlConfig)
    reward_config: RewardConfig = field(default_factory=RewardConfig)
    init_state: InitState = field(default_factory=InitState)
    commands: Commands = field(default_factory=Commands)
    normalization: Normalization = field(default_factory=Normalization)
    asset: Asset = field(default_factory=Asset)
    sensor: Sensor = field(default_factory=Sensor)
    sim_dt: float = 0.01   # 仿真时间步长 10ms
    ctrl_dt: float = 0.01  # 控制时间步长 10ms
```

---

## 附录 B: 关键代码片段

### 动作空间初始化（修复版）

```python
def _init_action_space(self):
    """
    初始化动作空间为归一化的 [-1, 1]

    说明：
    - 动作空间应该是归一化的，与物理限位无关
    - 物理安全由 action_scale 和 PD 控制器保证
    - 这样设计使得网络输出语义清晰：
      * 0 = 保持默认姿态
      * ±1 = 最大偏移 (±action_scale)
    """
    model = self.model
    self._action_space = gym.spaces.Box(
        -np.ones(model.num_actuators),  # [−1, −1, −1, ..., −1] (12个)
        np.ones(model.num_actuators),   # [+1, +1, +1, ..., +1] (12个)
        (model.num_actuators,),         # shape = (12,)
        dtype=np.float32,
    )
```

### 动作应用流程

```python
def apply_action(self, action):
    """
    应用动作到机器人

    Args:
        action: 策略网络输出，shape=(12,), 范围 [-1, 1]

    流程：
        1. action ∈ [-1, 1]  (from network)
        2. scaled = action × action_scale  (e.g., [-0.25, 0.25])
        3. target = scaled + default_angles  (e.g., [0.65, 1.15])
        4. tau = PD_controller(target, current_pos, current_vel)
        5. apply tau to simulator
    """
    # 缩放动作
    action_scale = self.cfg.control_config.action_scale
    scaled_action = action * action_scale

    # 加上默认角度
    target_pos = scaled_action + self._init_dof_pos

    # 应用到模型（后续由 PD 控制器处理）
    self.model.set_target_positions(target_pos)
```

---

## 附录 C: 数学公式

### 跟踪奖励（高斯核）

$$
r_{\text{tracking}} = \exp\left(-\frac{\|v_{\text{cmd}} - v_{\text{actual}}\|^2}{\sigma^2}\right)
$$

其中：
- $v_{\text{cmd}}$: 指令速度
- $v_{\text{actual}}$: 实际速度
- $\sigma = 0.25$: 核宽度（`tracking_sigma`）

**特性：**
- 完美跟踪: $r = 1$
- 误差为 $\sigma$: $r \approx 0.37$
- 误差为 $2\sigma$: $r \approx 0.02$

### PD 控制器

$$
\tau = K_p (q_{\text{target}} - q) - K_d \dot{q}
$$

其中：
- $\tau$: 输出力矩
- $q_{\text{target}}$: 目标关节角度
- $q$: 当前关节角度
- $\dot{q}$: 当前关节速度
- $K_p$: 比例增益
- $K_d$: 微分增益

### 动作空间变换

$$
q_{\text{target}} = s \cdot a + q_{\text{default}}
$$

其中：
- $s = 0.25$: action_scale
- $a \in [-1, 1]$: 网络输出动作
- $q_{\text{default}}$: 默认关节角度

**示例计算：**
```
a = 0.8
s = 0.25
q_default = 0.9

q_target = 0.25 × 0.8 + 0.9 = 1.1 rad
```

---

## 附录 D: 术语对照表

| 英文 | 中文 | 说明 |
|------|------|------|
| action space | 动作空间 | 策略可以输出的动作范围 |
| observation space | 观测空间 | 环境状态的表示空间 |
| action scale | 动作缩放系数 | 将归一化动作映射到实际角度偏移 |
| reward shaping | 奖励塑形 | 设计奖励函数引导学习 |
| policy | 策略 | 从状态到动作的映射（神经网络）|
| episode | 回合 | 从初始化到终止的一次完整交互 |
| step | 步 | 一次 action-observation 循环 |
| actuator | 执行器 | 关节电机 |
| DOF | 自由度 | Degree of Freedom |
| PD controller | PD控制器 | Proportional-Derivative 控制器 |
| domain randomization | 域随机化 | sim-to-real 的技术 |

---

好了！现在您有了完整的教学资料。如果还有任何疑问或需要更详细的说明，请随时告诉我！
