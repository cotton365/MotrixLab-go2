# Unitree Go2 平地行走任务迁移教程

我们现在要做的就是将 IsaacLab 的 unitree-go2-flat 任务迁移到 MotrixLab 上，即使用 MotrixLab 的框架，使用 IsaacLab 的代码也能跑通。

---

## 一、首先理解：什么是四足机器人的"控制"？

go2 机器人有四条腿，每个腿有三个关节：
- **hip（髋关节）**：控制腿的左右摆动
- **thigh（大腿关节）**：控制腿的前后摆动
- **calf（小腿关节）**：控制小腿的弯曲

**控制机器人走路的本质就是：每个时刻告诉这 12 个关节应该转到什么角度。**

### 训练算法

针对这个任务，我们设计的算法就是 **PPO**（一个 Actor 输出动作，一个 Critic 评估状态，通过反复试错让机器人学会走路）

---

## 二、奖励函数设计

针对这个任务，MotrixLab 和 IsaacLab 设计的奖励函数可以分成这三类：

### 1. 任务目标奖励（Task Rewards）
- **目的**：让机器人完成任务
- **例子**：跟随速度指令

### 2. 约束惩罚（Constraint Penalties）
- **目的**：避免不良行为
- **例子**：不要倾斜、不要跳跃

### 3. 正则化项（Regularization）
- **目的**：让动作更平滑、更节能
- **例子**：限制力矩、平滑动作变化

---

## 三、任务目标奖励（Task Rewards）

这类奖励告诉机器人"要做什么"，是训练的主要目标。

### 📊 tracking_lin_vel（跟踪线速度）

**物理含义**：机器人实际前进/侧移速度是否跟上指令速度

| 版本 | IsaacLab | Motrix 修改前 | Motrix 修改后 |
|------|----------|---------------|---------------|
| 权重 | 1.5 | 1.0 | **1.5** ✓ |

**迁移修正**：权重从 1.0 提升到 1.5，让机器人更重视速度跟踪（这是最主要的任务目标）

---

### 📊 tracking_ang_vel（跟踪角速度）

**物理含义**：机器人实际转向速度是否跟上指令

| 版本 | IsaacLab | Motrix 修改前 | Motrix 修改后 |
|------|----------|---------------|---------------|
| 权重 | 0.75 | 0.5 | **0.75** ✓ |

**迁移修正**：权重从 0.5 提升到 0.75，对齐 IsaacLab

---

### 📊 feet_air_time（足部腾空时间）

**物理含义**：脚抬起离地的时间，鼓励机器人有明显的"抬脚-落脚"动作

| 版本 | IsaacLab | Motrix 修改前 | Motrix 修改后 |
|------|----------|---------------|---------------|
| 权重 | 0.25 | 1.0 | **0.25** ✓ |

**迁移修正**：权重从 1.0 降低到 0.25，避免机器人为了腾空时间长而"跳着走"

---

## 四、约束惩罚（Constraint Penalties）

这类奖励告诉机器人"不要做什么"，确保行走姿态合理。

### ⚠️ orientation（姿态惩罚）

**物理含义**：机器人身体是否水平（不能左右倾斜或前后仰俯）

| 版本 | IsaacLab | Motrix 修改前 | Motrix 修改后 |
|------|----------|---------------|---------------|
| 权重 | -2.5 | **0.0** ❌ | **-2.5** ✓ |

**迁移修正**：⚠️ **这是最关键的修正！** 原版竟然没有姿态惩罚，修正后机器人必须保持直立行走

---

### ⚠️ lin_vel_z（z 方向速度惩罚）

**物理含义**：机器人不应该上下跳动

| 版本 | IsaacLab | Motrix 修改前 | Motrix 修改后 |
|------|----------|---------------|---------------|
| 权重 | (包含在其他项中) | -2.0 | -2.0 |

**说明**：保持不变，防止机器人跳跃

---

### ⚠️ ang_vel_xy（roll/pitch 角速度惩罚）

**物理含义**：机器人身体不应该左右摇晃或前后点头

| 版本 | IsaacLab | Motrix 修改前 | Motrix 修改后 |
|------|----------|---------------|---------------|
| 权重 | (包含在其他项中) | -0.05 | -0.05 |

**说明**：保持不变，确保稳定行走

---

## 五、正则化项（Regularization）

这类奖励让机器人的动作更平滑、更接近真实机器人的运动特性。

### 🔧 dof_acc（关节加速度惩罚）

**物理含义**：关节转速变化不能太剧烈（平滑运动）

| 版本 | IsaacLab | Motrix 修改前 | Motrix 修改后 |
|------|----------|---------------|---------------|
| 权重 | -2.5e-7 | -2.5e-7 | -2.5e-7 |

**说明**：已对齐，让关节运动更平滑

---

### 🔧 action_rate（动作变化率惩罚）

**物理含义**：网络输出的控制信号不能突变（避免"抽搐"）

| 版本 | IsaacLab | Motrix 修改前 | Motrix 修改后 |
|------|----------|---------------|---------------|
| 权重 | (未单独设置) | -0.001 | **-0.01** |

**迁移修正**：权重增强 10 倍，让动作输出更平滑（实践验证效果更好）

---

### 🔧 torques（力矩惩罚）

**物理含义**：电机输出力矩大小（力矩越小越节能）

| 版本 | IsaacLab | Motrix 修改前 | Motrix 修改后 |
|------|----------|---------------|---------------|
| 权重 | -0.0002 | -0.00001 | **0.0** |

**迁移修正**：⚠️ 设为 0（关闭）。原因：IsaacLab 和 Motrix 对力矩的计算方式不同，这是框架差异

---

## 六、Action Space 定义差异 ⚠️ 关键修正

### 什么是 Action Space？

**Action Space（动作空间）** 定义了神经网络可以输出的动作范围。

对于 Go2 机器人：
- 神经网络需要输出 12 个数字（对应 12 个关节）
- **问题**：这 12 个数字的取值范围应该是多少？

---

### 两种设计对比

#### IsaacLab 的设计（正确）✓

```python
action_space = [-1, 1]  # 所有关节统一范围
```

**设计理念**：
- Action 是**归一化**的（统一 [-1, 1]）
- 通过 `action_scale` 控制实际运动范围
- 清晰、对称、易于学习

**实际使用**：
```
目标角度 = action_scale × action + 默认角度
        = 0.25 × action + 默认角度
```

---

#### Motrix 修改前的设计（错误）❌

```python
action_space = actuator_ctrl_limits  # 使用物理关节限位
```

**实际值示例**：
```python
FL_calf: [-2.62, -0.85]  # 全是负值！
FL_hip:  [-1.05, 1.05]   # 对称
FL_thigh:[-0.66, 2.97]   # 不对称
```

**问题**：
- 每个关节范围不同
- 有的关节不对称（calf 全负值）
- 与 action_scale 的设计意图冲突

---

### 代码修正

#### 修改前（错误）：

```python
def _init_action_space(self):
    model = self.model
    self._action_space = gym.spaces.Box(
        np.array(model.actuator_ctrl_limits[0, :]),  # ❌ 使用物理限位
        np.array(model.actuator_ctrl_limits[1, :]),  # ❌
        (model.num_actuators,),
        dtype=np.float32,
    )
```

#### 修改后（正确）：

```python
def _init_action_space(self):
    model = self.model
    self._action_space = gym.spaces.Box(
        -np.ones(model.num_actuators),  # ✓ 统一为 -1
        np.ones(model.num_actuators),   # ✓ 统一为 +1
        (model.num_actuators,),
        dtype=np.float32,
    )
```

---

### 为什么这个修正很重要？

#### 问题 1：与 action_scale 的设计冲突

**正确的逻辑**：
```
网络输出 action ∈ [-1, 1]
    ↓
乘以 action_scale (0.25)
    ↓
得到偏移量 ∈ [-0.25, 0.25] rad
    ↓
加上默认角度
    ↓
最终目标角度
```

**错误的逻辑**：
```
网络输出 action ∈ [-2.62, -0.85]  # calf 关节
    ↓
乘以 action_scale (0.25)
    ↓
得到 [-0.655, -0.2125]  # ❌ 失去了控制作用
```

---

#### 问题 2：calf 关节的特殊问题

**物理限位**：
```python
calf 关节: [-2.721, -0.837] rad
转换为角度: [-155.9°, -48.0°]
```

这是硬件限制（小腿只能向后弯），**但不应该直接作为 action space**！

**如果用物理限位**：
- 神经网络只能输出负值
- 严重限制学习能力
- 语义混乱

**修正后**：
- 神经网络输出 [-1, 1]
- 实际角度通过 action_scale 和默认角度计算
- 仍在物理限位内 ✓

---

### 设计原则总结

| 特性 | IsaacLab | Motrix 修改前 | Motrix 修改后 |
|------|----------|---------------|---------------|
| **action space 范围** | `[-1, 1]` | `actuator_ctrl_limits` | `[-1, 1]` ✓ |
| **所有关节统一** | ✓ | ❌ | ✓ |
| **对称性** | ✓ | ❌ | ✓ |
| **与 action_scale 配合** | ✓ | ❌ | ✓ |
| **语义清晰** | ✓ | ❌ | ✓ |

**核心原则**：
> **Action Space 是给神经网络看的，应该归一化、对称、清晰。**
> **物理限位是给硬件看的，不应该直接暴露给神经网络。**

---

## 七、迁移要点总结

### ✅ 关键修正（必须调整）

1. **orientation: 0.0 → -2.5**
   - 最重要的修复！原版没有姿态约束

2. **action_space: actuator_ctrl_limits → [-1, 1]**
   - 唯一的函数设计层面修正

3. **tracking_lin_vel: 1.0 → 1.5**
   - 提升主要任务目标权重

4. **tracking_ang_vel: 0.5 → 0.75**
   - 对齐 IsaacLab

5. **feet_air_time: 1.0 → 0.25**
   - 避免跳着走

### ⚙️ 细节优化

6. **action_rate: -0.001 → -0.01**
   - 增强平滑控制

7. **torques: -0.00001 → 0.0**
   - 框架差异调整

---

## 八、给学生的建议

### 理解核心概念

**三个层次**：
1. **Action Space**：神经网络输出范围（应归一化为 [-1, 1]）
2. **Action Scale**：控制实际运动幅度（0.25）
3. **物理限位**：硬件极限（用于安全检查，不是 action space）

### 迁移的核心要点

**两大类修正**：
1. **奖励函数权重**：对齐任务目标和约束条件
2. **Action Space 定义**：从物理限位改为归一化范围

**关键点**：奖励函数告诉机器人"做什么、不做什么"，Action Space 定义了"怎么做"的基础框架。两者配合才能让机器人学会既快又稳的行走方式。
